name: Complete tests

on:
  workflow_dispatch:
  schedule:
    - cron: "0 12 * * 0"  # Weekly on Sunday at noon UTC
  pull_request:
    types: [synchronize, opened, reopened]
    branches:
      - main

env:
  KACHERY_CLOUD_CLIENT_ID: ${{ secrets.KACHERY_CLOUD_CLIENT_ID }}
  KACHERY_CLOUD_PRIVATE_KEY: ${{ secrets.KACHERY_CLOUD_PRIVATE_KEY }}

concurrency:  # Cancel previous workflows on the same pull request
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  run:
    name: ${{ matrix.os }} Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.12"]  # Lower and higher versions we support
        os: [macos-13, windows-latest, ubuntu-latest]
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v41

      - name: List all changed files
        shell: bash
        env:
          ALL_CHANGED_FILES: ${{ steps.changed-files.outputs.all_changed_files }}
        run: |
          for file in ${ALL_CHANGED_FILES}; do
            echo "$file was changed"
          done
      - name: Detect changed modules
        shell: bash
        run: |
          changed_files="${{ steps.changed-files.outputs.all_changed_files }}"
          python .github/detect_module_changes.py $changed_files

      - name: Print module changes
        shell: bash
        run: |
          echo "CORE_CHANGED=${CORE_CHANGED}"
          echo "EXTRACTORS_CHANGED=${EXTRACTORS_CHANGED}"
          echo "PLEXON2_CHANGED=${PLEXON2_CHANGED}"
          echo "PREPROCESSING_CHANGED=${PREPROCESSING_CHANGED}"
          echo "POSTPROCESSING_CHANGED=${POSTPROCESSING_CHANGED}"
          echo "QUALITYMETRICS_CHANGED=${QUALITYMETRICS_CHANGED}"
          echo "SORTERS_CHANGED=${SORTERS_CHANGED}"
          echo "SORTERS_EXTERNAL_CHANGED=${SORTERS_EXTERNAL_CHANGED}"
          echo "SORTERS_INTERNAL_CHANGED=${SORTERS_INTERNAL_CHANGED}"
          echo "COMPARISON_CHANGED=${COMPARISON_CHANGED}"
          echo "CURATION_CHANGED=${CURATION_CHANGED}"
          echo "WIDGETS_CHANGED=${WIDGETS_CHANGED}"
          echo "EXPORTERS_CHANGED=${EXPORTERS_CHANGED}"
          echo "SORTINGCOMPONENTS_CHANGED=${SORTINGCOMPONENTS_CHANGED}"
          echo "GENERATION_CHANGED=${GENERATION_CHANGED}"
      - name: Get current hash (SHA) of the ephy_testing_data repo

        id: repo_hash
        run: echo "dataset_hash=$(git ls-remote https://gin.g-node.org/NeuralEnsemble/ephy_testing_data.git HEAD | cut -f1)" >> $GITHUB_OUTPUT
        shell: bash
      - name: Cache datasets
        id: cache-datasets
        uses: actions/cache/restore@v4
        with:
          path: ~/spikeinterface_datasets
          key: ${{ runner.os }}-datasets-${{ steps.repo_hash.outputs.dataset_hash }}
          restore-keys: ${{ runner.os }}-datasets

      - name: Install packages
        run: |
          git config --global user.email "CI@example.com"
          git config --global user.name "CI Almighty"
          pip install -e .[test,extractors,streaming_extractors,full]
          pip install tabulate
        shell: bash

      - name: Installad datalad
        run: |
          pip install datalad-installer
          if [ ${{ runner.os }} = 'Linux' ]; then
            datalad-installer --sudo ok git-annex --method datalad/packages
          elif [ ${{ runner.os }} = 'macOS' ]; then
            datalad-installer --sudo ok git-annex --method brew
          elif [ ${{ runner.os }} = 'Windows' ]; then
            datalad-installer --sudo ok git-annex --method datalad/git-annex:release
          fi
          pip install datalad
          git config --global filter.annex.process "git-annex filter-process"  # recommended for efficiency
        shell: bash



      - name: Set execute permissions on run_tests.sh
        run: chmod +x .github/run_tests.sh
        shell: bash

      - name: Test core
        run: pytest -m "core"
        shell: bash

      - name: Test extractors
        env:
          HDF5_PLUGIN_PATH: ${{ github.workspace }}/hdf5_plugin_path_maxwell
        if: steps.modules-changed.outputs.EXTRACTORS_CHANGED == 'true' || steps.modules-changed.outputs.CORE_CHANGED == 'true'
        run: pytest -m "extractors"
        shell: bash

      - name: Test preprocessing
        if: steps.modules-changed.outputs.PREPROCESSING_CHANGED == 'true' || steps.modules-changed.outputs.CORE_CHANGED == 'true'
        run: ./.github/run_tests.sh "preprocessing and not deepinterpolation" --no-virtual-env
        shell: bash

      - name: Test postprocessing
        if: steps.modules-changed.outputs.POSTPROCESSING_CHANGED == 'true' || steps.modules-changed.outputs.CORE_CHANGED == 'true'
        run: ./.github/run_tests.sh postprocessing --no-virtual-env
        shell: bash

      - name: Test quality metrics
        if: steps.modules-changed.outputs.QUALITYMETRICS_CHANGED == 'true' || steps.modules-changed.outputs.CORE_CHANGED == 'true'
        run: ./.github/run_tests.sh qualitymetrics --no-virtual-env
        shell: bash

      - name: Test comparison
        if: steps.modules-changed.outputs.COMPARISON_CHANGED == 'true' || steps.modules-changed.outputs.GENERATION_CHANGED == 'true' || steps.modules-changed.outputs.CORE_CHANGED == 'true'
        run: ./.github/run_tests.sh comparison --no-virtual-env
        shell: bash

      - name: Test core sorters
        if: steps.modules-changed.outputs.SORTERS_CHANGED == 'true' || steps.modules-changed.outputs.CORE_CHANGED == 'true'
        run: ./.github/run_tests.sh sorters --no-virtual-env
        shell: bash

      - name: Test internal sorters
        if: steps.modules-changed.outputs.SORTERS_INTERNAL_CHANGED == 'true' || steps.modules-changed.outputs.SORTINGCOMPONENTS_CHANGED == 'true' || steps.modules-changed.outputs.CORE_CHANGED == 'true'
        run: ./.github/run_tests.sh sorters_internal --no-virtual-env
        shell: bash

      - name: Test curation
        if: steps.modules-changed.outputs.CURATION_CHANGED == 'true' || steps.modules-changed.outputs.CORE_CHANGED == 'true'
        run: ./.github/run_tests.sh curation --no-virtual-env
        shell: bash

      - name: Test widgets
        if: steps.modules-changed.outputs.WIDGETS_CHANGED == 'true' || steps.modules-changed.outputs.CORE_CHANGED == 'true' || steps.modules-changed.outputs.QUALITYMETRICS_CHANGED == 'true' || steps.modules-changed.outputs.PREPROCESSING_CHANGED == 'true'
        run: ./.github/run_tests.sh widgets --no-virtual-env
        shell: bash

      - name: Test exporters
        if: steps.modules-changed.outputs.EXPORTERS_CHANGED == 'true' || steps.modules-changed.outputs.CORE_CHANGED == 'true' || steps.modules-changed.outputs.WIDGETS_CHANGED == 'true'
        run: ./.github/run_tests.sh exporters --no-virtual-env
        shell: bash

      - name: Test sortingcomponents
        if: steps.modules-changed.outputs.SORTINGCOMPONENTS_CHANGED == 'true' || steps.modules-changed.outputs.CORE_CHANGED == 'true'
        run: ./.github/run_tests.sh sortingcomponents --no-virtual-env
        shell: bash

      - name: Test generation
        if: steps.modules-changed.outputs.GENERATION_CHANGED == 'true' || steps.modules-changed.outputs.CORE_CHANGED == 'true'
        run: ./.github/run_tests.sh generation --no-virtual-env
        shell: bash
