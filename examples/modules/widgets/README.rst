Widgets module
-------------------------------

The :code:`widgets` module imports the :code:`spikewidgets` package.
It contains several plotting routines (widgets) for visualizing recordings and sorting data, probe layout, and many more!

