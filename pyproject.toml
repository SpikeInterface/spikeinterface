[tool.poetry]
name = "spikeinterface"
version = "0.1.0"
description = ""
authors = ["Can H. Tartanoglu <canhtart@gmail.com>"]

[tool.poetry.dependencies]
python = ">=3.8,<3.11"
neo = "^0.10.0"
joblib = "^1.1.0"
tqdm = "^4.62.3"
probeinterface = "^0.2.6"

tridesclous = {version = "^1.6.4", optional = true}
PyQt5 = {version = "^5.15.6", optional = true}
loky = {version = "^3.0.0", optional = true}
MEArec = {version = "^1.7.2", optional = true}

spyking-circus = {version = "^1.1.0", optional = true, extras = ["spyking-circus"]}
herdingspikes = {version = "^0.3.99", optional = true, extras = ["herdingspikes"]}

[tool.poetry.dev-dependencies]
pytest = "^7.0.1"
MEArec = "^1.7.2"

# for testing
tridesclous = "^1.6.4"
PyQt5 = "^5.15.6"
loky = "^3.0.0"
spyking-circus = "^1.1.0"
herdingspikes = "^0.3.99"

[tool.poetry.extras]
tridesclous = ["tridesclous", "PyQt5", "loky"]


[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
